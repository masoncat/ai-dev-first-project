# 实操记录

日期：2026-08-23

## 环境

- Node.js：`v22.22.1`
- npm：`10.9.4`
- Git：`2.39.5`
- 浏览器：Chrome，通过本地地址 `http://127.0.0.1:4173` 打开

## 按文章步骤完成的闭环

1. 先写测试，再实现 `organizeText`。第一次运行测试时，得到模块不存在错误；补上 `public/organizer.js` 后，3 个测试全部通过。
2. 启动本地服务，桌面端输入示例文本，点击“整理文本”，得到标题、3 条关键要点和 2 条待确认问题。
3. 刷新页面后验证空输入，页面显示 `请先粘贴一段文字`，状态变为 `需要补充输入`。
4. 输入 5001 个字符后验证长度边界，页面显示 `文字长度不能超过 5000 字`，没有展示旧结果。
5. 调整浏览器到移动端宽度，确认输入区和结果区可以纵向排列，按钮仍然可操作。
6. 用 Git 保存第一个可运行版本，提交短哈希为 `8dce556`。

## 实操中发现的一个问题

最初在 `textarea` 上添加了 `maxlength="5000"`。这样浏览器会在输入阶段直接截断文字，业务代码无法收到超长输入，用户也看不到“超过 5000 字”的失败提示。删除 HTML 原生限制，改为由整理函数校验并由界面展示错误后，超长输入路径才真正可验证。

## 当前边界

本记录验证的是本地规则版，不代表在线模型调用、账号登录、云端保存或自动发布已经完成。Cursor 和 GitHub Copilot 的登录及扩展功能仍应按各自官方文档单独验证。
